{
  "$schema": "https://modelcontextprotocol.io/schema/mcp-config.schema.json",
  "mcpServers": {
    "memory": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-memory"
      ],
      "env": {
        "MEMORY_FILE_PATH": ".claude/state/memory.jsonl"
      }
    },
    "supabase": {
      "type": "http",
      "url": "https://mcp.supabase.com/mcp?project_ref=duhsbuyxyppgbkwbbtqg&read_only=false&features=database,docs,functions",
      "headers": {
        "Authorization": "Bearer ${SUPABASE_MCP_TOKEN}"
      }
    },
    "akg": {
      "command": "bun",
      "args": [
        "run",
        "packages/web/src/tools/akg/mcp/server.ts"
      ],
      "env": {
        "AKG_GRAPH_PATH": "docs/architecture/akg/graph/current.json",
        "AKG_DIAGRAMS_PATH": "docs/architecture/akg/diagrams",
        "AKG_PROJECT_ROOT": "."
      }
    },
    "context7": {
      "type": "http",
      "url": "https://mcp.context7.com/mcp",
      "headers": {
        "Authorization": "Bearer ${CONTEXT7_API_KEY}"
      }
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_TOKEN": "${GITHUB_TOKEN}"
      }
    },
    "cloudflare-docs": {
      "command": "npx",
      "args": ["-y", "mcp-remote", "https://docs.mcp.cloudflare.com/mcp", "--header", "Authorization:${CF_BEARER_TOKEN}"],
      "env": {
        "CF_BEARER_TOKEN": "Bearer ${CLOUDFLARE_API_TOKEN}"
      }
    },
    "cloudflare-bindings": {
      "command": "npx",
      "args": ["-y", "mcp-remote", "https://bindings.mcp.cloudflare.com/mcp", "--header", "Authorization:${CF_BEARER_TOKEN}"],
      "env": {
        "CF_BEARER_TOKEN": "Bearer ${CLOUDFLARE_API_TOKEN}"
      }
    },
    "cloudflare-observability": {
      "command": "npx",
      "args": ["-y", "mcp-remote", "https://observability.mcp.cloudflare.com/mcp", "--header", "Authorization:${CF_BEARER_TOKEN}"],
      "env": {
        "CF_BEARER_TOKEN": "Bearer ${CLOUDFLARE_API_TOKEN}"
      }
    },
    "cloudflare-builds": {
      "command": "npx",
      "args": ["-y", "mcp-remote", "https://builds.mcp.cloudflare.com/mcp", "--header", "Authorization:${CF_BEARER_TOKEN}"],
      "env": {
        "CF_BEARER_TOKEN": "Bearer ${CLOUDFLARE_API_TOKEN}"
      }
    },
    "cloudflare-logpush": {
      "command": "npx",
      "args": ["-y", "mcp-remote", "https://logs.mcp.cloudflare.com/mcp", "--header", "Authorization:${CF_BEARER_TOKEN}"],
      "env": {
        "CF_BEARER_TOKEN": "Bearer ${CLOUDFLARE_API_TOKEN}"
      }
    },
    "cloudflare-graphql": {
      "command": "npx",
      "args": ["-y", "mcp-remote", "https://graphql.mcp.cloudflare.com/mcp", "--header", "Authorization:${CF_BEARER_TOKEN}"],
      "env": {
        "CF_BEARER_TOKEN": "Bearer ${CLOUDFLARE_API_TOKEN}"
      }
    }
  }
}
